# face_recognition_models imports pkg_resources at runtime, which setuptools
# removed in 81. Keep this pin until that package is patched or vendored.
setuptools<81

opencv-python==5.0.0.93
numpy==2.5.3

# Pulls in dlib==20.0.1 as a dependency.
face_recognition==1.3.0

# Not on PyPI: the model .dat files must come from git.
face_recognition_models @ git+https://github.com/ageitgey/face_recognition_models.git
